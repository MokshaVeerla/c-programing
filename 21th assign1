#include<stdio.h>
main()
{
	int s=0,r=0,n,m;
	printf("enter a number");
	scanf("%d",&n);
	m=n;
	while(n!=0)
	{
    r=n%10;
	s=(s*10)+r;
	n= n/10;
}
	{
		if(s==m)
		printf("PALINDROME");
		else
		printf("NOT A PALINDROME");
	}
}
